Run command

```bash
python interface/interface_job.py -c [PATH-CONFIG] -e [PATH-ENV] -n [NUM-QUERY]
```

For `PATH-CONFIG`, please see `interface/app_configs/config_base.toml`.
For `PATH-ENV`, please see `.env_base`.

# Langfuse

Follow the instruction at [langfuse-onprem](https://langfuse.com/self-hosting/deployment/docker-compose).
